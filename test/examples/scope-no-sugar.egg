do(
  define(x,9),
  # def crea una nueva variable local
  define(f, fun(
    do(
      define(x, 4),
      print(x) # 4
    )
  )),
  # set no crea una nueva variable local
  define(g, fun(set(x, 8))),
  f(),
  print(x), # 9
  g(),
  print(x) # 8
)